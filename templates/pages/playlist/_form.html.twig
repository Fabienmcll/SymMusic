{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="bg-yellow-500 text-white hover:bg-yellow-600 hover:underline py-1 px-3 rounded ml-2 transition mb-2">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
